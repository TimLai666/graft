# Build artifacts
*.exe
*.test
*.out

# Editor/OS noise
.DS_Store
Thumbs.db

# Claude Code session artifacts (worktrees, etc.)
.claude/

# Regenerable reference dumps and golden-diff artifacts
testdata/sheet-*.png
testdata/golden/*.got.png

# Runtime logs
*.log

# Local workaround: patched gogpu/ui fork wired via go.work replace, pending
# upstream fix (gogpu/ui#133). Not committed — see go.work.
.patched/
